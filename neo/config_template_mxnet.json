{
    "CompilationJobName": "${jobname}",
    "RoleArn": "${your_sagemaker_role_arn}",
    "InputConfig": {
        "S3Uri": "s3://${your_bucket}/${your_input_folder}/tf_frcnn.tar.gz",
        "DataInputConfig":  "{'data': [1,3,224,224]}",
        "Framework": "MXNET"
    },
    "OutputConfig": {
        "S3OutputLocation": "s3://${your_bucket}/${your_output_folder}",
        "TargetDevice": "rasp3b"
    },
    "StoppingCondition": {"MaxRuntimeInSeconds": 1800}
}
